# Replace these values on the deployment host. Never commit real credentials.
POSTGRES_USER=personal_finance
POSTGRES_PASSWORD=replace-with-a-strong-password
POSTGRES_DB=personal_finance
# URL-encode credentials here if they contain reserved URL characters.
DATABASE_URL=postgresql://personal_finance:replace-with-a-strong-password@database:5432/personal_finance?schema=public
# Example only: choose a release whose image publication workflow succeeded.
IMAGE_TAG=v0.1.0
WEB_PORT=8080
